<div class="container">
  <div class="text-center">
    <h1>🥐 <%= @restaurant.name %></h1>
    <p><%= @restaurant.address %></p>
    <p><%= @restaurant.phone_number %></p>
  </div>

  <div class="container" id="reviews">
    <ul>
      <% @restaurant.reviews.each do |review| %>
      <div class="row">
        <div class="col-sm-2" id="rating">
          <%= raw("<i class='fas fa-star'></i>" * review.rating) %>
          </div>
          <div class="col-sm-10 text-right"><%= review.content %></div>
        </div>
      <% end %>
    </ul>
  </div>
    <div class="row">
      <div class="text-center"><%= link_to "🍏 Add a review", new_restaurant_review_path(@restaurant) %></div>
    </div>
</div>